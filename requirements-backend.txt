# ==============================================================================
# BACKEND MICROSERVICE — FastAPI + LangGraph Agent + Guardrails + Gateway
# Excludes: eval packages (ragas, deepeval, sentence-transformers), Streamlit,
#           heavy ingestion parsers (unstructured, poppler)
# ==============================================================================

# --- CORE API ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
requests==2.33.1
numpy==2.4.4
pytz==2026.2

# --- GOOGLE CLOUD ---
google-cloud-storage==3.10.1
google-cloud-aiplatform==1.151.0
cloud-sql-python-connector==1.20.2
pg8000==1.31.5
sqlalchemy==2.0.49

# --- POSTGRES (LangGraph PostgresSaver) ---
psycopg[binary]
psycopg-pool

# --- VECTOR DB & RETRIEVAL ---
qdrant-client==1.17.1
FlashRank==0.2.10

# --- REDIS (Semantic Cache) ---
redis==7.4.0

# --- LANGCHAIN & AGENTIC ORCHESTRATION ---
langchain==1.2.18
langgraph==1.1.10
langgraph-checkpoint-postgres
langchain-google-vertexai==3.2.3
langchain-groq==1.1.2
langchain-community==0.4.1
langchain-openai==1.2.1
pydantic==2.13.4

# --- GUARDRAILS ---
nemoguardrails==0.21.0
langchain-nvidia-ai-endpoints==1.3.0

# --- LLM GATEWAY ---
portkey-ai==2.3.0

# --- OBSERVABILITY ---
langsmith==0.8.3
logfire[fastapi,requests]==4.32.1
loguru==0.7.3

# --- MINIMAL INGESTION PARSERS (for inline parsing during startup) ---
pypdf==6.11.0
beautifulsoup4==4.14.3
